{
    "interval-seconds": "10",
    "on-fail": [
        {
            "type": "email",
            "email": "$PONG_EMAIL_TARGET",
            "username": "$PONG_EMAIL_USERNAME",
            "password": "$PONG_EMAIL_PASSWORD"
        }
    ]
}